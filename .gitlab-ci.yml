# Copyright (c) 2024, NVIDIA CORPORATION.  All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

variables:
  GIT_TERMINAL_PROMPT: 1
  NGC_ORG: nv-ngc-devops
  NGC_USER_NAME: '$$oauthtoken'
  NVCR_CONTAINER_REPO: nvcr.io
  NSPECT_ID: NSPECT-JRZ4-EI8F
  # Add Go module cache path
  GOPATH: $CI_PROJECT_DIR/.go
  GO_CACHE_DIR: $CI_PROJECT_DIR/.go-cache

  HELM_URL: https://helm.ngc.nvidia.com
  KANIKO_IMAGE: "gcr.io/kaniko-project/executor:v1.19.2-debug"

  # When using dind service, you must instruct docker to talk with the
  # daemon started inside of the service. The daemon is available with
  # a network connection instead of the default /var/run/docker.sock socket.
  #
  # The 'docker' hostname is the alias of the service container as described at
  # https://docs.gitlab.com/ee/ci/docker/using_docker_images.html#accessing-the-services
  #
  # If you're using GitLab Runner 12.7 or earlier with the Kubernetes executor and Kubernetes 1.6 or earlier,
  # the variable must be set to tcp://localhost:2375 because of how the
  # Kubernetes executor connects services to the job container
  # DOCKER_HOST: tcp://localhost:2375
  #
  DOCKER_HOST: tcp://docker:2375
  #
  # This instructs Docker not to start over TLS.
  DOCKER_TLS_CERTDIR: ""

image: $CI_REGISTRY_IMAGE/ci:29082025

include:
  - project: dgxcloud/mk8s/gitlab-templates
    ref: main
    file: mr-cop-template.yml

  # - component: gitlab-master.nvidia.com/dgxcloud/platform/gitlab-components/security-scans/sonarqube-scan@main
  #   inputs:
  #     sonar_project_key: "nv-sentinel" 
  #     allow_failure: true

  - component: gitlab-master.nvidia.com/dgxcloud/release-automation/gitlab-components/security-scans/secret-scan@main
    inputs:
      stage: "security"
      allow_failure: true
      scan_directory: "."

  # - component: gitlab-master.nvidia.com/dgxcloud/release-automation/gitlab-components/security-scans/container-scan@main
  #   inputs:
  #     stage: "security"
  #     needs: 
  #       - prepare-vars
  #       - build-image-list
  #       - job: log-collector-container-publish
  #         optional: true
  #       - job: nic-health-monitor-container-publish
  #         optional: true
  #       - job: nvswitch-health-monitor-container-publish
  #         optional: true
  #       - job: platform-connectors-container-publish
  #         optional: true
  #       - job: health-event-analyzer-container-publish
  #         optional: true
  #       - job: fault-quarantine-module-container-publish
  #         optional: true
  #       - job: node-drainer-module-container-publish
  #         optional: true
  #       - job: fault-remediation-module-container-publish
  #         optional: true
  #       - job: health-event-client-container-publish
  #         optional: true
  #       - job: node-health-events-uds-connector-container-publish
  #         optional: true
  #       - job: kubernetes-distro-helm-publish
  #         optional: true
  #     nspect_id: NSPECT-JRZ4-EI8F
  #     policy: "container_scan_policy.json"
  #     image_names: ["nvcr.io/nv-ngc-devops/nvsentinel-gpu-health-monitor", "nvcr.io/nv-ngc-devops/nvsentinel-nic-health-monitor", "nvcr.io/nv-ngc-devops/nvsentinel-nvswitch-health-monitor", "nvcr.io/nv-ngc-devops/nvsentinel-platform-connectors", "nvcr.io/nv-ngc-devops/nvsentinel-fault-quarantine-module", "nvcr.io/nv-ngc-devops/nvsentinel-node-health-events-uds-connector-server", "nvcr.io/nv-ngc-devops/busybox", "nvcr.io/nv-ngc-devops/nvsentinel-health-events-analyzer", "nvcr.io/nv-ngc-devops/nvsentinel-node-drainer-module", "nvcr.io/nv-ngc-devops/nvsentinel-fault-remediation-module", "nvcr.io/nv-ngc-devops/nvsentinel-health-event-client", "nvcr.io/nv-ngc-devops/rtsp-mongosh", "nvcr.io/nv-ngc-devops/nvsentinel-log-collector"]
  #     is_release: false
  #     allow_failure: true
  #     platform_archs: ["linux/amd64"]
  #     # versions.txt is generated by the build-image-list job and contains the list of container images with their versions
  #     image_versions_file_path: "versions.txt"  

  # - component: gitlab-master.nvidia.com/dgxcloud/release-automation/gitlab-components/security-scans/open-source-scan@main
  #   inputs:
  #     stage: "security"
  #     nspect_id: "NSPECT-JRZ4-EI8F"
  #     gitlab_token: $DGXC_RELEASE_BOT_TOKEN
  #     allow_failure: true

  - component: gitlab-master.nvidia.com/dgxcloud/release-automation/gitlab-components/nspect/nspect-program-version@1.0.8
    inputs:
      stage: "release"
      nspect_id: $NSPECT_ID
      container_images_file: "versions.txt"
      needs: 
        - build-image-list
      source_details:
        - sourcePointer: "${CI_PROJECT_URL}.git"
          componentName: "${CI_PROJECT_NAME}"
          branch: "$CI_COMMIT_BRANCH"
      program_version: "${CI_COMMIT_TAG}"

      gitlab_token: $DGXC_RELEASE_BOT_TOKEN
      rules: 
        - if: $CI_COMMIT_TAG

stages:
  - .pre
  - check
  - lint
  - test
  - generate
  - publish
  - deploy
  - release
  - security
  
workflow:
  rules:
    - if: $CI_PIPELINE_SOURCE == 'merge_request_event'
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    
.eks-runner-tags:
  tags:
    - eks-build

# =================================================================================================
.dind-service:
  image: docker:28.1.1
  services:
    - docker:28.1.1-dind
  tags:
    - eks-dind
  variables:
    DOCKER_HOST: tcp://docker:2376
    DOCKER_DRIVER: overlay2
    DOCKER_TLS_VERIFY: true
    DOCKER_TLS_CERTDIR: /builds/certs
    DOCKER_CERT_PATH: /builds/certs/client


prepare-vars:
  stage: .pre  # or stage: init
  script:
    - SAFE_REF_NAME=$(echo $CI_COMMIT_REF_NAME | sed 's/\//-/g')
    - echo "SAFE_REF_NAME=$SAFE_REF_NAME" >> variables.env
  artifacts:
    reports:
      dotenv: variables.env

build-image-list:
  stage: .pre
  # This job generates versions.txt which contains a list of all container images and their versions
  # The file is generated by build_image_list.sh which:
  # 1. Reads image repository and tag information from distros/kubernetes/nvsentinel/values.yaml
  # 2. Combines consumed and built image lists
  # 3. Outputs the final list to versions.txt
  script:
    - export SAFE_REF_NAME=$(echo "$CI_COMMIT_REF_NAME" | sed 's/\//-/g')
    - chmod +x ./build_image_list.sh
    - ./build_image_list.sh
    - cat versions.txt
  artifacts:
    paths:
      - versions.txt
  needs:
    - prepare-vars

mrcop:
  stage: check
  extends:
    .mrcop

protos-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - protoc -I protobufs/ --go_out=platform-connectors/pkg/protos/ --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative --go-grpc_out=platform-connectors/pkg/protos/ protobufs/platformconnector.proto
    - protoc -I protobufs/ --go_out=health-monitors/nic-health-monitor/pkg/protos/ --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative --go-grpc_out=health-monitors/nic-health-monitor/pkg/protos/ protobufs/platformconnector.proto
    - protoc -I protobufs/ --go_out=health-monitors/nvswitch-health-monitor/pkg/protos/ --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative --go-grpc_out=health-monitors/nvswitch-health-monitor/pkg/protos/ protobufs/platformconnector.proto
    - protoc -I protobufs/ --go_out=platform-connectors/pkg/connectors/nodehealtheventsudsconnector/protos/ --go_opt=paths=source_relative --go-grpc_opt=paths=source_relative --go-grpc_out=platform-connectors/pkg/connectors/nodehealtheventsudsconnector/protos/ protobufs/nodehealtheventsudsconnector.proto
    - python3 -m grpc_tools.protoc -Iprotobufs/ --python_out=health-monitors/gpu-health-monitor/gpu_health_monitor/platform_connector/protos --pyi_out=health-monitors/gpu-health-monitor/gpu_health_monitor/platform_connector/protos --grpc_python_out=health-monitors/gpu-health-monitor/gpu_health_monitor/platform_connector/protos protobufs/platformconnector.proto
    - python3 -m grpc_tools.protoc -Iprotobufs/ --python_out=health-monitors/gpu-health-monitor/gpu_health_monitor/clear_xid_errors/protos --pyi_out=health-monitors/gpu-health-monitor/gpu_health_monitor/clear_xid_errors/protos --grpc_python_out=health-monitors/gpu-health-monitor/gpu_health_monitor/clear_xid_errors/protos protobufs/platformconnector.proto
    - sed -i 's/^import platformconnector_pb2 as platformconnector__pb2$/from . import platformconnector_pb2 as platformconnector__pb2/' health-monitors/gpu-health-monitor/gpu_health_monitor/platform_connector/protos/platformconnector_pb2_grpc.py # Due to this issue https://github.com/protocolbuffers/protobuf/issues/1491, this needs to be done else it will throw error during execution
    - sed -i 's/^import platformconnector_pb2 as platformconnector__pb2$/from . import platformconnector_pb2 as platformconnector__pb2/' health-monitors/gpu-health-monitor/gpu_health_monitor/clear_xid_errors/protos/platformconnector_pb2_grpc.py # Due to this issue https://github.com/protocolbuffers/protobuf/issues/1491, this needs to be done else it will throw error during execution
    - git status --porcelain --untracked-files=no
    - git --no-pager diff
    - '[ -z "$(git status --porcelain --untracked-files=no)" ]'

license-headers-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - addlicense -f license-header.txt -check -ignore **/*lock.hcl -ignore **/*pb2.py -ignore **/*pb2_grpc.py -ignore **/*.csv .

# argo-workflow-test:
#   stage: test
#   extends:
#     - .eks-runner-tags
#   script:
#     - apt-get update -qq
#     - DEBIAN_FRONTEND=noninteractive apt-get install -y -qq python3-venv
#     - python3 -m venv /tmp/venv
#     - . /tmp/venv/bin/activate
#     - pip install --quiet pytest ruamel.yaml python-gitlab
#     - cd argo-workflow
#     - python -m pytest deployment-workflow/scripts/tests/

gpu-health-monitor-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/gpu-health-monitor
    - poetry install
    - poetry run black --check .

gpu-health-monitor-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/gpu-health-monitor
    - poetry install
    - poetry run coverage run --source=gpu_health_monitor -m pytest -vv --junitxml=report.xml
    - poetry run coverage xml || true
    - poetry run coverage report || true
  artifacts:
    when: always
    reports:
      junit: health-monitors/gpu-health-monitor/report.xml
      coverage_report:
        coverage_format: cobertura
        path: health-monitors/gpu-health-monitor/coverage.xml
  coverage: '/TOTAL.*? (100(?:\.0+)?\%|[1-9]?\d(?:\.\d+)?\%)$/'

gpu-health-monitor-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-monitors/gpu-health-monitor
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-gpu-health-monitor:${SAFE_REF_NAME}-dcgm-3.x --build-arg DCGM_VERSION=3.3.7-1-ubuntu22.04 --build-arg PYTHONPATH=/usr/local/dcgm/bindings/python3 . -f Dockerfile
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host  --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-gpu-health-monitor:${SAFE_REF_NAME}-dcgm-4.x . -f Dockerfile 

nic-health-monitor-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/nic-health-monitor
    - go vet ./...
    - golangci-lint run
  artifacts:
    when: always
    reports:
      codequality: nic-health-monitor/code-quality-report.json
    paths:
      - nic-health-monitor/code-quality-report.json

nic-health-monitor-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/nic-health-monitor
    - gotestsum --junitfile report.xml -- -race -short $(go list ./... | grep -v pkg/protos) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: nic-health-monitor/coverage.xml
      junit: nic-health-monitor/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

nic-health-monitor-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-monitors/nic-health-monitor
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-nic-health-monitor:${SAFE_REF_NAME} . 
  
syslog-health-monitor-lint:
  stage: lint
  script:
    - cd health-monitors/syslog-health-monitor
    - go vet ./...
    - golangci-lint run
  artifacts:
    when: always
    reports:
      codequality: syslog-health-monitor/code-quality-report.json
    paths:
      - syslog-health-monitor/code-quality-report.json

syslog-health-monitor-test:
  stage: test
  script:
    - cd health-monitors/syslog-health-monitor
    - gotestsum --junitfile report.xml -- -race -short $(go list ./... | grep -v pkg/protos) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: syslog-health-monitor/coverage.xml
      junit: syslog-health-monitor/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

syslog-health-monitor-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-monitors/syslog-health-monitor
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-syslog-health-monitor:${SAFE_REF_NAME} .
  tags:
    - dind
    
nvswitch-health-monitor-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/nvswitch-health-monitor
    - go vet ./...
    - golangci-lint run
  artifacts:
    when: always
    reports:
      codequality: nvswitch-health-monitor/code-quality-report.json
    paths:
      - nvswitch-health-monitor/code-quality-report.json

nvswitch-health-monitor-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd health-monitors/nvswitch-health-monitor
    - gotestsum --junitfile report.xml -- -race -short $(go list ./... | grep -v pkg/protos) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: nvswitch-health-monitor/coverage.xml
      junit: nvswitch-health-monitor/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

nvswitch-health-monitor-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-monitors/nvswitch-health-monitor
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-nvswitch-health-monitor:${SAFE_REF_NAME} . 

csp-health-monitor-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - cd health-monitors/csp-health-monitor
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: health-monitors/csp-health-monitor/code-quality-report.json
    paths:
      - health-monitors/csp-health-monitor/code-quality-report.json

csp-health-monitor-test:
  stage: test
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd health-monitors/csp-health-monitor
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: csp-health-monitor/coverage.xml
      junit: csp-health-monitor/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

csp-health-monitor-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-monitors/csp-health-monitor
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-csp-health-monitor:${SAFE_REF_NAME} . 

health-event-client-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd health-event-client
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: health-event-client/code-quality-report.json
    paths:
      - health-event-client/code-quality-report.json

health-event-client-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd health-event-client
    - gotestsum --junitfile report.xml -- -race -short $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: health-event-client/coverage.xml
      junit: health-event-client/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

health-event-client-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-event-client
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-health-event-client:${SAFE_REF_NAME} .

platform-connectors-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd platform-connectors
    - go vet ./...
    - golangci-lint run
  artifacts:
    when: always
    reports:
      codequality: platform-connectors/code-quality-report.json
    paths:
      - platform-connectors/code-quality-report.json

platform-connectors-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd platform-connectors
    - gotestsum --junitfile report.xml -- -race -short $(go list ./... | grep -v -e pkg/protos -e pkg/connectors/nodehealtheventsudsconnector/protos) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: platform-connectors/coverage.xml
      junit: platform-connectors/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

platform-connectors-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd platform-connectors
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-platform-connectors:${SAFE_REF_NAME} . 


health-event-analyzer-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd health-events-analyzer
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: health-events-analyzer/code-quality-report.json
    paths:
      - health-events-analyzer/code-quality-report.json

health-event-analyzer-test:
  stage: test
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd health-events-analyzer
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: health-evens-analyzer/coverage.xml
      junit: health-events-analyzer/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

health-event-analyzer-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd health-events-analyzer
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-health-events-analyzer:${SAFE_REF_NAME} .


fault-quarantine-module-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd fault-quarantine-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: fault-quarantine-module/code-quality-report.json
    paths:
      - fault-quarantine-module/code-quality-report.json

fault-quarantine-module-test:
  stage: test
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd fault-quarantine-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: fault-quarantine-module/coverage.xml
      junit: fault-quarantine-module/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

fault-quarantine-module-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd fault-quarantine-module
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-fault-quarantine-module:${SAFE_REF_NAME} .


node-drainer-module-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd node-drainer-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: node-drainer-module/code-quality-report.json
    paths:
      - node-drainer-module/code-quality-report.json

node-drainer-module-test:
  stage: test
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd node-drainer-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - export PATH="$GOPATH/bin:$PATH"
    - go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
    - export KUBEBUILDER_ASSETS=$(setup-envtest use -p path 1.30.0)
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: node-drainer-module/coverage.xml
      junit: node-drainer-module/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

node-drainer-module-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd node-drainer-module
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-node-drainer-module:${SAFE_REF_NAME} .

    
fault-remediation-module-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd fault-remediation-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - go vet ./...
    - golangci-lint run
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      codequality: fault-remediation-module/code-quality-report.json
    paths:
      - fault-remediation-module/code-quality-report.json

fault-remediation-module-test:
  stage: test
  variables:
    GIT_PRIVATE_DOMAIN: gitlab-master.nvidia.com
  script:
    - cd fault-remediation-module
    - echo "machine $GIT_PRIVATE_DOMAIN login gitlab-ci-token password $CI_JOB_TOKEN" > ~/.netrc
    - export GOPRIVATE=$GIT_PRIVATE_DOMAIN/dgxcloud/mk8s/*
    - go install sigs.k8s.io/controller-runtime/tools/setup-envtest@latest
    - export KUBEBUILDER_ASSETS=$(setup-envtest use -p path 1.30.0)
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  after_script:
    - rm -f ~/.netrc
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: fault-remediation-module/coverage.xml
      junit: fault-remediation-module/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

fault-remediation-module-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd fault-remediation-module
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --build-arg GITLAB_HOST=gitlab-master.nvidia.com --build-arg GITLAB_LOGIN=gitlab-ci-token --secret id=gitlab_token,env=CI_JOB_TOKEN --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-fault-remediation-module:${SAFE_REF_NAME} .


node-health-events-uds-connector-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -f distros/container/nvsentinel-node-health-events-distro/Dockerfile -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-node-health-events-uds-connector-server:${SAFE_REF_NAME} . 


store-client-sdk-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd store-client-sdk
    - go vet ./...
    - golangci-lint run
  artifacts:
    when: always
    reports:
      codequality: store-client-sdk/code-quality-report.json
    paths:
      - store-client-sdk/code-quality-report.json

store-client-sdk-test:
  stage: test
  extends:
    - .eks-runner-tags
  script:
    - cd store-client-sdk
    - gotestsum --junitfile report.xml -- -race $(go list ./...) -coverprofile=coverage.txt -covermode atomic
    - go tool cover -func coverage.txt 
    - gocover-cobertura < coverage.txt > coverage.xml
  artifacts:
    when: always
    reports:
      coverage_report:
        coverage_format: cobertura
        path: store-client-sdk/coverage.xml
      junit: store-client-sdk/report.xml
  coverage: '/total:\s*\(statements\)\s*\d+.\d+%/'

kubernetes-distro-lint:
  stage: lint
  extends:
    - .eks-runner-tags
  script:
    - cd distros/kubernetes
    - helm lint nvsentinel/


kubernetes-distro-helm-publish:
  stage: publish
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - cd distros/kubernetes
    - helm repo add --force-update ngc ${HELM_URL}/${NGC_ORG} --username=${NGC_USER_NAME} --password=${NGC_PASSWORD}
    - helm repo add --force-update mongodb ${HELM_URL}/${NGC_ORG} --username=${NGC_USER_NAME} --password=${NGC_PASSWORD}
    - cd nvsentinel/charts/mongodb-store && helm dependency update && cd ../../..
    - cd nvsentinel && helm dependency update && cd ..
    - helm package nvsentinel --version ${CI_COMMIT_TAG}
    - helm cm-push -f nvsentinel-${CI_COMMIT_TAG}.tgz ngc
    - helm repo add --force-update ngc ${HELM_URL}/${NGC_ORG} --username=${NGC_USER_NAME} --password=${NGC_PASSWORD}
    - helm cm-push -f nvsentinel-${CI_COMMIT_TAG}.tgz ngc

log-collector-container-publish:
  stage: publish
  extends:
    - .dind-service
  timeout: 15 minutes
  retry:
    max: 2
    when:
      - job_execution_timeout
      - script_failure
  before_script:
    - docker info
    - docker login nvcr.io -u ${NGC_USER_NAME} -p ${NGC_PASSWORD}
  script:
    - cd nvsentinel-log-collector
    - docker context create builder
    - docker buildx create builder --driver-opt network=host --buildkitd-flags '--allow-insecure-entitlement network.host' --use
    - docker buildx build --platform linux/arm64,linux/amd64 --network=host --push -t ${NVCR_CONTAINER_REPO}/${NGC_ORG}/nvsentinel-log-collector:${SAFE_REF_NAME} .

log-collector-lint:
  stage: lint
  image: koalaman/shellcheck-alpine:stable
  extends:
    - .eks-runner-tags
  script:
    - cd nvsentinel-log-collector
    - shellcheck entrypoint.sh 
