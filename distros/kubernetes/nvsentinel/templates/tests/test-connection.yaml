apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "nvsentinel.fullname" . }}-test-connection"
  labels:
    {{- include "nvsentinel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "nvsentinel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
