apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "nvsentinel.serviceAccountName" . }}
  labels:
    {{- include "nvsentinel.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
